{
  "name": "mathraids",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "wrangler deploy --dry-run"
  },
  "dependencies": {
    "phaser": "^4.0.0-rc.5"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
